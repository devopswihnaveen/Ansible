- name: Print variable with variable preference
  hosts: localhost
  connection: local
  # vars:
  #   name: "Argument from play Variable"
  # vars_files:
  # - variables.yaml
  # vars_prompt:
    # name: "name"
    # prompt: "Please enter your name:"
    # private: no
  tasks:
  - name: print variable with variable preference
    # vars:
    #   name: "Ansible from Task Variable"
    ansible.builtin.debug:
      msg: "Hi {{ name }}. Good Morning. Welcome to Ansible Automation"


##Variable Preference in Ansible:

## 1. Variable from Arguments
## 2. Variable from Task
## 3. Variable from vars_files
## 4. Variable from vars_prompt    
## 5. Variable from Play Variable
## 6. Variable from inventory
## 7. Variable from facts